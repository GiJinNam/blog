spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa

  h2:
    console:
      enabled: true

  jpa:
    defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate:
        formal_sql:true

jwt:
  issuer: everday16@gmail.com
  secret_key: study_springboot
